# Explicit SPA routes - serve the clean SPA shell via the .htm fallback file.
# Since Cloudflare Pages only strips .html, the .htm target is served directly without pretty-url redirects.
/dashboard/*       /spa.htm 200
/dashboard         /spa.htm 200
/login-or-signup   /spa.htm 200
/q/*               /spa.htm 200
